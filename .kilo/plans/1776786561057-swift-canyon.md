# Plan to Fix Invisible Associations in Use Case Diagram

## Problem Analysis
The user reports that associations `Agent --> UC8` and `Manager --> UC8` are not visible in the diagram, while all other elements and associations are perfect. The PlantUML code in `cdu.puml` correctly defines these associations on lines 96-97. The custom HTML diagram in `pgi_performance_anip_use_case_diagram.html` includes corresponding SVG lines, but they may be hidden due to initial opacity settings in the GSAP animation.

## Investigation Steps
1. **Verify Rendering Method**: Determine if the user is viewing the diagram via:
   - Standard PlantUML renderer (VS Code extension, online tool, etc.)
   - The custom HTML file with JavaScript animation
   - Exported image/PDF from PlantUML

2. **Check for Rendering Issues**: 
   - If using PlantUML renderer, test with different layout engines (e.g., Graphviz, Smetana)
   - If using HTML, ensure JavaScript is enabled and animation has played to step 3 where these links become visible
   - Check for overlapping elements or short line lengths

## Proposed Solutions
1. **For PlantUML Rendering**:
   - Add `!pragma layout smetana` at the top of the file for better layout
   - Change `left to right direction` to `top to bottom direction` to potentially improve association visibility
   - Add explicit positioning hints if needed (though not ideal for use case diagrams)

2. **For Custom HTML Diagram**:
   - Modify initial GSAP settings to make `.link-agent-eval` and `.link-man-eval` visible from start (opacity: 0.5)
   - Ensure the animation timeline reaches step 3 where these links are revealed

3. **Alternative Layout Adjustments**:
   - Move UC8 outside its package temporarily to test if package boundaries are causing visibility issues
   - Adjust actor positioning or use case arrangement

## Implementation Priority
- Start with quick fixes: add layout pragma or change direction in PlantUML
- If using HTML, ensure animation plays correctly
- Only modify package structure as last resort since user considers current layout perfect

## Verification
After changes, regenerate the diagram and confirm both associations are clearly visible without affecting other elements.