# Plan: Fix Content Structure and HTML Entities Issues

## Overview
Fix critical content issues in the CahierDesCharges component: replace HTML entities with proper characters and restructure sections to match the original document format.

## Current State Analysis
**HTML Entities Issue:**
- "&apos;" characters appearing throughout the text instead of proper apostrophes (')
- This affects readability and professional appearance

**Structure Issues:**
- "Utilisateurs du système" is currently a card within "Autres points clés du projet" but should be its own main section
- "Évolutions possibles" is currently a card within "Indicateurs de succès & Évolutions" but should be its own section titled "Évolutions possibles à moyen terme"
- Missing proper section separation that matches the original document structure

## Objectives
1. **Fix HTML Entities**: Replace all "&apos;" with proper apostrophes (')
2. **Restructure Sections**: Move "Utilisateurs du système" and "Évolutions possibles" to be independent main sections
3. **Maintain Visual Design**: Keep the recent UI/UX improvements while fixing content structure

## Implementation Plan

### Phase 1: HTML Entities Cleanup
**Files to modify:**
- `src/components/CahierDesCharges.tsx` - Replace all "&apos;" with proper apostrophes

**Changes:**
- Search and replace all instances of "&apos;" with "'"
- Ensure proper French apostrophes are used throughout
- Test that text renders correctly without HTML entities

### Phase 2: Section Structure Restructuring
**Files to modify:**
- `src/components/CahierDesCharges.tsx` - Restructure sections to match original document

**Changes:**
- Extract "Utilisateurs du système" from the "Autres points clés du projet" section and make it a standalone Section
- Extract "Évolutions possibles" from "Indicateurs de succès & Évolutions" and create new section "Évolutions possibles à moyen terme"
- Rename "Indicateurs de succès & Évolutions" to just "Indicateurs de succès"
- Update section icons and IDs appropriately
- Ensure proper section ordering: 7. Utilisateurs du système, 8. Indicateurs de succès, 9. Évolutions possibles à moyen terme

### Phase 3: Content Verification
**Files to modify:**
- `src/components/CahierDesCharges.tsx` - Verify content matches original document

**Changes:**
- Cross-reference with original document structure
- Ensure all sections from original document are present
- Verify section numbering and titles match original format
- Check that content within each section is properly organized

## Technical Considerations
- Maintain existing UI/UX improvements (uppercase titles, card headers, etc.)
- Preserve all animations and responsive behavior
- Ensure proper TypeScript typing and component structure
- Keep accessibility and semantic HTML intact

## Validation Steps
1. Test build process to ensure no compilation errors
2. Verify all HTML entities are properly replaced
3. Check section structure matches original document
4. Test responsive behavior on different screen sizes
5. Ensure animations and interactions still work
6. Verify text readability and professional appearance

## Potential Challenges
- Ensuring all HTML entities are found and replaced (may be in multiple locations)
- Maintaining proper section hierarchy and visual flow
- Preserving existing styling while restructuring content
- Ensuring no content is lost during restructuring