# Plan: Final UI Polish - Underline Titles and Remove Card Icons

## Overview
Apply final UI polishing touches to the CahierDesCharges component: add subtle underlines to main section titles and remove unnecessary icon props from card headers.

## Current State Analysis
**Main Title Styling:**
- Section titles are currently uppercase, centered, and larger
- Could benefit from subtle underline for better visual emphasis
- Need to ensure underline doesn't interfere with existing styling

**Card Icon Cleanup:**
- Cards with titles now display titles in dedicated headers
- Icon props are still being passed to Card components but not displayed
- These icon props are now redundant and should be removed for cleaner code
- Cards like "Piloter la Performance", "Optimiser & Engager", etc. have unused icon props

## Objectives
1. **Add Subtle Underlines**: Apply elegant underlines to main section titles for enhanced visual hierarchy
2. **Clean Up Card Icons**: Remove redundant icon props from Card components since icons are no longer displayed in headers
3. **Maintain Design Consistency**: Ensure changes complement existing modern, soft aesthetic

## Implementation Plan

### Phase 1: Add Title Underlines
**Files to modify:**
- `src/components/CahierDesCharges.module.css` - Update `.sectionTitle` styling

**Changes:**
- Add subtle underline to section titles using `border-bottom` or `text-decoration`
- Choose elegant styling that fits the modern, soft design aesthetic
- Ensure underline works well with centered layout and existing spacing
- Test that underline doesn't interfere with responsive design

### Phase 2: Remove Card Icon Props
**Files to modify:**
- `src/components/CahierDesCharges.tsx` - Remove icon props from Card components

**Changes:**
- Identify all Card components that have title props (display in headers)
- Remove `icon={IconName}` props from these Card components
- Keep icons for cards that don't have titles (if any still exist)
- Ensure TypeScript types still work correctly after prop removal

### Phase 3: Visual Testing
**Files to modify:**
- Visual inspection and responsive testing

**Changes:**
- Verify underline styling looks good on different screen sizes
- Confirm card headers still display properly without icons
- Check that overall visual hierarchy is maintained
- Ensure no layout shifts or styling conflicts

## Technical Considerations
- Maintain existing animations and hover effects
- Preserve accessibility (underline shouldn't affect screen readers)
- Keep responsive behavior intact
- Ensure no TypeScript errors after prop removal

## Validation Steps
1. Test build process to ensure no compilation errors
2. Verify underline styling on different screen sizes
3. Check that card headers display correctly without icons
4. Ensure animations and interactions still work
5. Confirm visual hierarchy and readability are maintained

## Potential Challenges
- Finding the right underline styling that complements the design
- Ensuring prop removal doesn't break component functionality
- Maintaining visual balance with the new underline elements
- Testing responsive behavior with underlines