# Plan to Make pgi_performance_anip_use_case_diagram.html Accessible on Vercel Deployment

## Goal
Determine if deploying the Next.js platform on Vercel allows access to the HTML diagram file via a link, and provide steps to achieve accessibility.

## Analysis
- The project is a Next.js application, confirmed by package.json and project structure.
- Next.js serves static files from the `public/` directory at the root URL (e.g., `public/file.html` is accessible at `/file.html`).
- The HTML file is currently in `docs/`, which is not part of the public directory and thus not served by Next.js or Vercel.
- The HTML file references `ANIP-Logo-HD.png`, which exists in `assets/` but needs path adjustment for correct serving.
- Vercel supports Next.js deployments and will serve static files from `public/` without issues.
- The HTML is self-contained with external dependencies (CDNs), so it will render correctly as a static page.

## Steps to Make Accessible
1. **Move the HTML file**: Relocate `docs/pgi_performance_anip_use_case_diagram.html` to `public/docs/pgi_performance_anip_use_case_diagram.html`.
2. **Update image paths**: Change `<img src="ANIP-Logo-HD.png"` to `<img src="/assets/ANIP-Logo-HD.png"` to correctly reference the logo in `public/assets/`.
3. **Ensure assets are in place**: Confirm `public/assets/ANIP-Logo-HD.png` exists (it does, based on project files).
4. **Deploy on Vercel**: Use Vercel's deployment process for the Next.js app. The static HTML will be served at the URL `https://yourvercelapp.com/docs/pgi_performance_anip_use_case_diagram.html`.

## Verification
- After deployment, test the link to ensure the diagram loads and functions (including interactive elements via GSAP).
- If any relative paths to other assets are incorrect, update them accordingly.

## Conclusion
Yes, by moving the file to `public/` and adjusting paths, it will be accessible via a direct link on Vercel.</content>
<parameter name="filePath">.kilo/plans/1776788455981-eager-cabin.md