# Plan: Animations Story-Panel ANIP

## Objectif
Implémenter des animations cinématiques pour le story-panel avec text reveal et synchronisation fluide.

## État actuel
-Story-panel CSS lignes 406-439
-Fonction `updateStoryUI` lignes 844-863
-GSAP déjà chargé

## Tâches planifiées

### 1. Amélioration CSS du story-panel
- Ajouter styles améliorés: `font-weight: 600-700`, `letter-spacing`, `text-shadow` légère
- Ajouter backdrop-filter增强 ( blur plus fort)
- Ajouter structure pour split-text par caractère
- **Durée**: 0.6s (Cinéma)
- **Mode**: Présentation uniquement
- **FIX BUG**: Ajouter `white-space: pre-wrap` sur `#story-step-desc` pour préserver les espaces
- **NOUVEAU FIX**: Grouper les caractères par mot (pas par caractère unique) pour éviter les mots coupés

### 2. Fonction Text Reveal (par mot, pas par caractère)
- ~~Modifier `updateStoryUI` pour splitter le texte en caractères~~
- **CORRIGÉ**: Grouper le texte par mots avec `text.split(' ')`
- Appliquer l'animation reveal mot par mot (plus naturel et pas de mots coupés)

### 3. Animation Cinéma (Entrée/Sortie)
- **Entrée**: blur(20px) → blur(0), slide-up (y:20→0), fade (0→1)
- **Sortie**: blur(0→20), slide-up (0→-20), fade (1→0)
- Durée: 0.4-0.5s avec ease `power2.out`

### 4. Synchronisation sortie/entrée
- Détecter si texte déjà affiché
- Avant d'afficher nouveau: lancer animation sortie de l'ancien
- only then afficher nouveau avec entrée
- Utiliser `gsap.killTweensOf()` pour éviter conflits

## Fichiers à modifier
`/home/albert/makuette/ANIP-Benin/ANIP-Benin/public/docs/pgi_performance_anip_use_case_diagram.html`

## Questions en suspens
- ~~Durée animation~~ → 0.6s (Cinéma)
- ~~Text reveal~~ → Lettre par lettre
- ~~Mode~~ → Mode présentation uniquement

## Bug détecté après analyse
Les espaces sont invisibles car `.char { display: inline-block }` collapse les whitespace HTML. Fix nécessaire.

## Bug #2 détecté par utilisateur
Les mots sont coupés/mal branchés lors du passage à la ligne - les caractères individuels font que les mots se cassent n'importe où.

**Solution**: Grouper les caractères par mot (ou par segments) plutôt que par caractère unique, afin que le retour à la ligne se fasse mot complet.
→ **FIXÉ**: Changement de `createCharSpans()` pour utiliser des mots au lieu de caractères

## Bug #3 détecté par utilisateur
Les animations sont trop rapides/dures. Vouloir les rendre plus douces et plus lentes.

**Solution**: Augmenter les durées d'animation dans le code JavaScript
- **CHOISI**: Durée d'entrée: 1.2s, Stagger: 0.15s
→ À implémenter dans `animateEntrance()` et sortie