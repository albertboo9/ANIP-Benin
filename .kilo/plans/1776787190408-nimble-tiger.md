# Plan : Correction des liaisons Agent/Manager --> UC8 invisibles

## Problème
Les lignes SVG reliant Agent et Manager au cas d'utilisation UC8 (Évaluation Continue) ne sont pas visibles lors du chargement du diagramme.

## Diagnostic
1. Les liens `link-agent-eval` et `link-man-eval` sont initialisés à `opacity: 0.5` (ligne 763)
2. Ils ciblent les coordonnées `y2="524"` au centre du module Evaluation
3. Les lignes passent **derrière** les éléments `.module` dans le DOM
4. L'ordre de rendu SVG les masque

## Corrections à appliquer dans `pgi_performance_anip_use_case_diagram.html`

### 1. Réorganiser les lignes SVG critiques
Déplacer `link-agent-eval` et `link-man-eval` **après** tous les autres éléments `.actor-link` dans le SVG pour améliorer le z-order de rendu.

### 2. Ajuster les coordonnées cibles
Les coordonnées actuelles (`x2="677" y2="524"`) ciblent le centre du module. Ajuster pour cibler le UC pill UC8 plutôt que le centre du module.

Coordonnées actuelles :
- `link-agent-eval`: x1="68" y1="222" x2="677" y2="524"
- `link-man-eval`: x1="58" y1="400" x2="677" y2="524"

### 3. Ajouter un style visible
```css
.link-agent-eval, .link-man-eval { 
  stroke-opacity: 0.8 !important; 
}
```

### 4. Corriger l'initialisation GSAP
S'assurer que ces liens sont visibles dès l'étape 0 (initialisation).

## Fichiers à modifier
- `/home/albert/makuette/ANIP-Benin/ANIP-Benin/docs/pgi_performance_anip_use_case_diagram.html`